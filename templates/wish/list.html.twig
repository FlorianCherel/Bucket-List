{% extends 'base.html.twig' %}

{% block title %}Bucket-List{% endblock %}

{% block body %}
    <style>
        .example-wrapper {
            margin: 1em auto;
            max-width: 800px;
            width: 95%;
            font: 18px/1.5 sans-serif;
        }

        .example-wrapper code {
            background: #F5F5F5;
            padding: 2px 6px;
        }
    </style>

    <div class="wishesList">

        <h1>Wishes List !</h1>
        <ul>
            {% for wish in wishes %}
                <li>
                    <span>{{ wish.title }}</span>
                    <a href="{{ path('app_wish_detail', {'id': wish.id}) }}"><button type="button" class="btn btn-outline-info">Détail</button></a>
                </li>
            {% endfor %}
        </ul>
    </div>
{% endblock %}